---
interface Props {
  small?: boolean
}

const { small } = Astro.props

const size = small
  ? 'text-4xl sm:text-6xl'
  : 'text-4xl sm:text-7xl md:text-8xl lg:text-9xl'
---

<h1 class={`uppercase font-light tracking-widest ${size} animate-fadeInDown`}>
  Nuno Góis
</h1>

<style>
  h1 {
    background: linear-gradient(to right, #5cd68f, #d65c5e);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: hue 10s infinite linear;
  }
  @keyframes hue {
    from {
      filter: hue-rotate(-360deg);
    }
    to {
      filter: hue-rotate(0deg);
    }
  }
</style>
